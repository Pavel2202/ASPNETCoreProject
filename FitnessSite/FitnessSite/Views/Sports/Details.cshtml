@model SportsDetailsViewModel

@{
    ViewData["Title"] = @Model.Name;
}

<div class="row">
    <div class="col-md-6 offset-4">
        <h2>@Model.Name</h2>
    </div>
</div>

<div class="row">
    <div class="col-md-6 offset-4">
        <h5>Origin: @Model.Origin</h5>
    </div>
</div>

<div class="row">
    <div class="col-md-10 mb-3">
        @Model.Description
    </div>
</div>